/**
 * The environment variable name an OpenAPI import writes request URLs against.
 *
 * Kept in its own module so the two import modals and the toggle can share it
 * without importing components from each other.
 */
export const DEFAULT_BASE_URL_VARIABLE = "baseUrl"
